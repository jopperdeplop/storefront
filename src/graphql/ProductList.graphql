query ProductList($channel: String!, $first: Int, $filter: ProductFilterInput, $locale: LanguageCodeEnum!) {
	products(channel: $channel, first: $first, filter: $filter) {
		edges {
			node {
				...ProductListItem
			}
		}
	}
}
