fragment ProductListItem on Product {
	id
	name
	slug
	thumbnail(size: 512, format: WEBP) {
		url
		alt
	}
	category {
		id
		name
	}
	pricing {
		priceRange {
			start {
				gross {
					amount
					currency
				}
			}
		}
	}
	translation(languageCode: $locale) {
		name
	}
}
