query PageGetBySlug($slug: String!, $locale: LanguageCodeEnum!) {
	page(slug: $slug) {
		id
		slug
		title
		seoTitle
		seoDescription
		content
		# --- NEW: Page Translation ---
		translation(languageCode: $locale) {
			title
			content
			seoTitle
			seoDescription
		}
	}
}
