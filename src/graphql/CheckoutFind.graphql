query CheckoutFind($id: ID!) {
	checkout(id: $id) {
		id
		email
		lines {
			id
			quantity
			totalPrice {
				gross {
					amount
					currency
				}
			}
			variant {
				product {
					id
					name
					slug
					thumbnail {
						url
						alt
					}
					category {
						name
					}
					# Translation removed to prevent 500 Server Error
				}
				pricing {
					price {
						gross {
							amount
							currency
						}
					}
				}
				name
				id
			}
		}
		totalPrice {
			gross {
				amount
				currency
			}
		}
	}
}
